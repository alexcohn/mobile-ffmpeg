name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: echo
      run: export ANDROID_NDK_ROOT=${ANDROID_SDK_ROOT}/ndk-bundle && ./android.sh
      shell: bash
